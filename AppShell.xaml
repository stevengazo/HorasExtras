<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="HorasExtras.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:HorasExtras"
    xmlns:Views="clr-namespace:HorasExtras.Views"
    Shell.FlyoutBehavior="Disabled"
    Shell.TabBarForegroundColor="White"
    Shell.TabBarUnselectedColor="{StaticResource Secondary}"
    Shell.TabBarTitleColor="White"
    Shell.TabBarBackgroundColor="{StaticResource Primary}"
    Shell.UnselectedColor="WhiteSmoke"
    Title="HorasExtras">

    <TabBar
        
        >
        <ShellContent
            
            Title="Proyectos"
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage"
            Icon="workpeople.png"/>
        <ShellContent
            Title="Exportar"
            ContentTemplate="{DataTemplate Views:Export}"
            Route="Exports"
            Icon="pdf.png"/>
        <ShellContent
            Title="Empleados"
            ContentTemplate="{DataTemplate Views:Employees}"
            Route="Employees"
            Icon="employess.png"/>
        <ShellContent
            Title="Configuración"
            ContentTemplate="{DataTemplate Views:Configuration}"
            Route="Configuration"
            Icon="settings.png"/>
    </TabBar>




</Shell>
